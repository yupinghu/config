[include]
    path = .gitconfig-more
[user]
    name = Yu Ping Hu
[core]
    editor = vi
    excludesfile = ~/config/gitignore
    autocrlf = input
[color]
    ui = auto
[alias]
    cp = cherry-pick
    co = checkout
    amend = commit --amend --date= .
    r = review
    draft = review -D
    lg = log --oneline --graph --decorate
    update = remote update -p
    review = push gerrit HEAD:refs/for/master
    submit = push origin HEAD:master
[push]
	default = simple
